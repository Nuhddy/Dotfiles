- dotfiles managed in stow packages

- automated deployment through ansible

- modular per-stow-package lists of dependencies (pacman/aur packages)

- config containing custom rules for which stow packages to deploy (named
  groups, shell checks, etc.)
